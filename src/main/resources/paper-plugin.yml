name: TNTWars
version: ${version}
author: Jeroeno_Boy
description: The Minecraft TNTWars Plugin
main: com.jeroenvdg.tntwars.TNTWars
api-version: '1.21'

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false

    FastAsyncWorldEdit:
      load: BEFORE
      required: true
      join-classpath: true